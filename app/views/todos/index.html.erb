<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/app/assets/bootstrap.min.css" rel="stylesheet">

  </head>
  <body>
    <div class="container">
      <%= render :partial=> "title"%>
      <div class="list">
      <!-- <table class="table table-striped table-condensed "> -->
        <%@todos.each do |todo|%>
          <ul class="list-word list-group">
            <li class="list-group-item">
              <% if (todo.date < Date.today)%>
                <span class="glyphicon glyphicon-remove kuku">&#160</span>
              <% else%>
                <span class="glyphicon glyphicon-ok kuku">&#160</span>
              <%end%>
              <span class="date-w">Date: <%=todo.date%></span>
              <div class="little-control">
              <ul>
                <li>
                  <%= link_to todo_path(todo),method: :delete,data:{confirm: "Are you sure?"} do%>
                    <span class="glyphicon glyphicon-fire"></span>
                  <%end%>
                </li>
                <li>
                  <%= link_to edit_todo_path(todo) do%>
                    <span class="glyphicon glyphicon-pencil"></span>
                  <%end%>
                </li>
                <li>
                  <%= link_to todo_path(todo) do%>
                    <span class="glyphicon glyphicon-eye-open"></span>
                  <%end%>

                </li>
              </ul>
              </div>
              <br><span>&#160&#160&#160&#160</span>
              <%=todo.customer%>

              <!-- <span class="edi"><%=link_to 'Edit' ,edit_todo_path(todo)%></span> -->

            </li>
            <!-- <td><%link_to 'Show' ,todo_path(todo)%></td>
            <td><%link_to 'Edit' ,edit_todo_path(todo)%></td>
            <td><%link_to 'Delete',todo_path(todo),method: :delete,data:{confirm: "Are you sure?"} %></td> -->
          </ul>
        <%end%>
        <!-- </table> -->


      </div>
    </div>
  </body>
</html>
